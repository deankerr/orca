{
  // Root is not a typecheck project — each app/package owns its own tsconfig.
  // Shared compiler defaults live in @orca/config/tsconfig.base.json.
  "extends": "@orca/config/tsconfig.base.json",
  "files": [],
  "include": [],
  "exclude": ["repos"]
}

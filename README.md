# ORCA

OpenRouter Capability Analysis

Exploring the model menagerie of OpenRouter.

## Environment

Set `ORCA_CRAWL_CRON_ENABLED=true` only in environments that should run scheduled Convex snapshot crawls.

When unset, automatic crawls stay off by default, which keeps preview deployments from continuing to gather data after their branch has already been merged.

{
  "name": "entity-logo-service",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "deploy": "bun run build && wrangler deploy",
    "dev": "bun run build && wrangler dev",
    "review": "bun run src/review.ts",
    "test": "bun test",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@lobehub/icons-static-avatar": "1.14.0",
    "@lobehub/icons-static-webp": "1.94.0",
    "@orca/config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "sharp": "^0.35.3",
    "typescript": "catalog:",
    "wrangler": "^4.131.1",
    "zod": "catalog:"
  }
}
